<div class="item-wrapper">
	<div class="item-header">
		<a data-link data-uuid="{{item.uuid}}">
			<img class="item-image" src="{{item.img}}">
		</a>
		<div class="item-text-wrapper" {{#if description}}data-action="expandDescription"{{/if}}>
			<span class="item-name">
				{{item.name}}

			</span>
			<span class="item-subtext">
				{{formatSubtext subtext attackType}}
			</span>
		</div>
		{{#if description}}<i class="fas fa-caret-right"></i>{{/if}}
	</div>

	<div class="description{{#if expanded}} expanded{{/if}}">
		<div class="description-wrapper">
			{{{description}}}
		</div>
	</div>
</div>
