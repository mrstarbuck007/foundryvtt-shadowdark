{{> chat/item-summary }}
{{{description}}}
{{#if isIdentified}}
	{{#each spells}}
		<span class="spell-name">
			{{this.name}}
		</span>
		<span class="item-subtext">
			{{subtext this.subtext}}
		</span>
	{{/each}}
{{/if}}
